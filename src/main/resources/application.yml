spring:
  application:
    name: jobSearch

  jackson:
    serialization:
      indent-output: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
